{
  "db": {
    "name": "db",
    "connector": "memory"
  },
  "postgres": {
    "host": "${dbHost}",
    "port": "${dbPort}",
    "database": "${dbDatabase}",
    "password": "${dbPassword}",
    "name": "postgres",
    "user": "${dbUser}",
    "connector": "postgresql",
    "ssl": false,
    "idleTimeoutMillis": 60000,
    "max": 5,
    "min": 2
  }
}
