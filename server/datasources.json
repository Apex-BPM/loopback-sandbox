{
  "db": {
    "name": "db",
    "connector": "memory"
  },
  "postgres": {
    "host": "${dbHost}",
    "port": "${dbPort}",
    "database": "${dbDatabase}",
    "password": "${dbPassword}",
    "name": "postgres",
    "user": "${dbUser}",
    "connector": "postgresql"
  }
}
